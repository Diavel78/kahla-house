# Environment
.env
*.env.local

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# Firebase service account
firebase-service-account.json

# IDE
.vscode/
.idea/
# Ignore everything inside .claude/ EXCEPT skills/ — project-level
# skills (e.g. the /handicap skill that drives the in-chat handicapper)
# need to follow the repo so every Claude Code session (incl. mobile)
# has them. Personal session state (settings.local.json, etc.) stays
# ignored.
.claude/*
!.claude/skills/

# OS
.DS_Store
Thumbs.db

# Data files
*.xlsx
*.csv
*.sqlite
*.db
output/
.vercel
