# Environment
.env
*.env.local

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# Firebase service account
firebase-service-account.json

# IDE
.vscode/
.idea/
# Ignore everything inside .claude/ EXCEPT what's listed below — project-
# level config that needs to follow the repo so every Claude Code session
# (incl. mobile) has them. Personal session state (settings.local.json)
# stays ignored.
.claude/*
!.claude/skills/
!.claude/hooks/
!.claude/settings.json

# OS
.DS_Store
Thumbs.db

# Data files
*.xlsx
*.csv
*.sqlite
*.db
output/
.vercel

# env backups — secrets, never committed (Sep 20 2026: a git add -A swept these
# into a commit and GitHub secret scanning blocked the push, correctly)
.env.bak*
