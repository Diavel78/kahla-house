flask>=3.0.0
polymarket-us>=0.1.0
requests>=2.31.0
python-dotenv>=1.0.0
firebase-admin>=6.4.0
# Cautious ceiling only -- 2.29.0 is simply unverified against this app.
# NOT the cause of anything: the Aug 25 2026 outage
# (`InvalidArgument: 400 Invalid database id %28default%29`) was a
# SERVER-side change and reproduced identically on 2.28.1, which is why
# the real fix is the routing-header shim in app.py. Pinning this does
# not protect against that class of break. Verify any bump with
# /api/firebase-probe, which does the exact read that failed.
google-cloud-firestore>=2.21.0,<2.30.0
supabase>=2.7.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
rapidfuzz>=3.9
# RSA-PSS signing for the authed Kalshi portfolio reader (fill status).
cryptography>=42.0
