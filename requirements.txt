flask>=3.0.0
polymarket-us>=0.1.0
requests>=2.31.0
python-dotenv>=1.0.0
firebase-admin>=6.4.0
# ⚠ PIN — THE Aug 25 2026 SITE OUTAGE. google-api-core 2.35.0 added
# `urllib.parse.quote(val, safe="/")` inside path_template.expand().
# Firestore builds its database string with that function:
#     expand('projects/{project}/databases/{database}', database='(default)')
#   2.34.0 -> projects/kahla-house/databases/(default)     works
#   2.35.0 -> projects/kahla-house/databases/%28default%29 REJECTED
# That string is the `parent` of every request, so EVERY Firestore read
# died with `InvalidArgument: 400 Invalid database id %28default%29`,
# /api/me 500'd, and the site became a click->bounce loop. api-core is a
# TRANSITIVE dep and nothing here was pinned, so a rebuild took prod down
# with no code change. Do not loosen without checking path_template.py
# and confirming with /api/firebase-probe.
google-api-core<2.35.0
supabase>=2.7.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
rapidfuzz>=3.9
# RSA-PSS signing for the authed Kalshi portfolio reader (fill status).
cryptography>=42.0
